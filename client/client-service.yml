apiVersion: v1
kind: Service
metadata:
  name: budget-tracker-react-service
  namespace: bootcamp3
spec:
  type: LoadBalancer
  selector:
    app: budget-tracker-react
  ports:
    - port: 80
      targetPort: 80
